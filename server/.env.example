# ========== SIMPLE LOGOUT-ALL (.env.example) ==========
# Copiar a .env y llenar con valores reales

# ========== SERVER ==========
PORT=3001
NODE_ENV=development

# ========== JWT ==========
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ========== DATABASE (MongoDB) ==========
# Local development
MONGODB_URI=mongodb://localhost:27017/logout-all

# O usar MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/logout-all

# ========== REDIS (solo si usas versión full) ==========
# No necesario para simple-logout-all
# REDIS_URL=redis://localhost:6379

# ========== SERVER ID (para multi-instance) ==========
# Cada instancia debe tener un ID único
SERVER_ID=server-1

# ========== NOTA ==========
# Para desarrollo simple:
# - Solo necesitas: PORT, JWT_SECRET, MONGODB_URI
# - Redis no es obligatorio
# - SERVER_ID es por si escalas a N servidores
